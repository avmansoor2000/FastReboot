// const bcrypt = require('bcrypt');

// const decryptPassword = async (hashedPassword) => {
//     // const decryptedPassword = await bcrypt.compare('testpassword', hashedPassword);
//     const decryptedPassword = await decryptPassword(hashedPassword)
//     return decryptedPassword ? 'testpassword' : 'Password cannot be decrypted';
// };

// module.exports = decryptPassword;
